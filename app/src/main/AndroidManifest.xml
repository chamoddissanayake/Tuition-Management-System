<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.tuitionmanagementsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".searchFeedback"></activity>
        <activity android:name=".MainFeedback" />
        <activity android:name=".TeacherRegistration"></activity>
        <activity android:name=".MainFeedback" />
        <activity android:name=".ContactUs" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Locate_Us" />
        <activity android:name=".TeacherSearchResults" />
        <activity android:name=".TeacherUpdateDeleteResults" />
        <activity android:name=".TeacherAddResults" />
        <activity android:name=".TeacherResultManagementDashboard" />
        <activity android:name=".StudentResultManagementDashboard" />
        <activity android:name=".EditRegistration_Details" />
        <activity android:name=".StudentRegistration" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StudentDashboard" />
        <activity android:name=".TeacherDashboard" />
        <activity android:name=".FeedBack" />
        <activity android:name=".Notification" />
        <activity android:name=".Feedbacknext" />
        <activity android:name=".UpdateDeleteNotification" />
        <activity android:name=".ViewFeedback" />
        <activity android:name=".ViewNotification" />
        <activity android:name=".Todolist" />
        <activity android:name=".Homework" />
        <activity android:name=".AddTutorials" />
        <activity android:name=".StudyMaterial" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>